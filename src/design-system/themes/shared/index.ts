/**
 * Shared Themes Export
 * Default shared theme configurations and utilities
 */

export {
  lightSharedTheme,
  darkSharedTheme,
  defaultSharedThemeConfig,
  sharedThemeStrategy,
  createSharedTheme,
  fallbackTheme,
  validateSharedTheme,
  sharedThemeCustomizations,
} from './default';

// Default export
export { default } from './default';
