/**
 * UI Atoms - Basic building blocks
 * 
 * Atoms are the smallest components that can't be broken down further.
 * They include basic HTML elements like buttons, inputs, labels, etc.
 */

// Export all atoms here as they are migrated
export { Button } from './Button';
export { Avatar } from './Avatar';
export { Icon } from './Icon';
export { Input } from './Input';
export { Typography } from './Typography';
export { LoadingSpinner } from './LoadingSpinner';
export { BackgroundPattern } from './BackgroundPattern';
