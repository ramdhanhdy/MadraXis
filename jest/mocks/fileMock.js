/**
 * File Mock for Jest Tests
 * 
 * Mocks static file imports (images, videos, audio, etc.) that cannot be processed
 * by Jest's default transformer. Returns a mock string for testing purposes.
 */

module.exports = 'test-file-stub';
